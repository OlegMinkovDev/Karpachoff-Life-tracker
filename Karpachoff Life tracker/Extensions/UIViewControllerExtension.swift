//
//  UIViewControllerExtension.swift
//  Karpachoff Life tracker
//
//  Created by Oleg Minkov on 21/12/2017.
//  Copyright © 2017 Oleg Minkov. All rights reserved.
//

import UIKit

extension UIViewController {
    
    /*func showAlert(withTitle title: String, message: String, andHandler handler: ((UIAlertAction) -> Void)?) {
        
        let alertController = UIAlertController(title: "Новая категория", message: "Введите название новой категории", preferredStyle: .alert)
        
        alertController.addTextField(configurationHandler: { (_textField) in
            textField?.text = _textField.text
            textField?.placeholder = "Название"
        })
        
        let cancelAction = UIAlertAction(title: "Отмена", style: .cancel, handler: nil)
        let doneAction = UIAlertAction(title: "Ок", style: .default, handler: handler)
        
        alertController.addAction(cancelAction)
        alertController.addAction(doneAction)
        
        DispatchQueue.main.async {
            self.present(alertController, animated: true, completion: nil)
        }
    }
    
    func showAlert(withTitle title: String, message: String, andAction action: UIAlertAction) {
        
        let alertController = UIAlertController(title: "Новая категория", message: "Введите название новой категории", preferredStyle: .alert)
        
        alertController.addTextField(configurationHandler: { (_textField) in
            textField?.text = _textField.text
            textField?.placeholder = "Название"
        })
        
        let cancelAction = UIAlertAction(title: "Отмена", style: .cancel, handler: nil)
        let doneAction = UIAlertAction(title: "Ок", style: .default, handler: handler)
        
        alertController.addAction(cancelAction)
        alertController.addAction(doneAction)
        
        DispatchQueue.main.async {
            self.present(alertController, animated: true, completion: nil)
        }
    }*/
}
